#include <stdio.h>
int main(){
    int a, b, c, max;
    printf("Enter 3 nos. : \n");
    scanf("%d%d%d",&a,&b,&c);
    max=a;
    if(b>max)
    //{
    max=b;
    if(c>max)
    //{
    max=c;
    printf("max : %d",max);
    /*printf("max: %d",c);
    return 0;
    }
    printf("max: %d",b);
    return 0;
    }
    printf("max: %d",a);*/
    return 0;
}
